server {
    listen 81 default_server;
    listen [::]:81 default_server;

    location / {
        fastcgi_pass 127.0.0.1:8080;
        include fastcgi_params;
    }

    location /status {
      stub_status;
    }
}
